# Keep railway up uploads small: only site/ source + public assets are needed
# (service root directory is /site). Evidence docs and raw generations stay in git.
# Patterns are anchored (leading /) where an unanchored form would also match
# inside site/ — e.g. plain "app/" would exclude site/app and break the build.
node_modules/
.next/
/assets/
/site/docs/
/app/
